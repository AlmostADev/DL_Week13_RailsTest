<h2><%= link_to 'Crear ToDo', todos_new_path %></h2>
<% @todos.each do |t| %>
  <div class="card bg-faded">
    <%= t.description %>
    <h2><%= link_to 'Mostrar ToDo', todo_path %></h2>
  </div>
<% end %>